<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Visitor</title>
  </head>
  <body>
    <form action="/visitor/edit?_method=patch" method="post">
      <fieldset>
        <legend>방명록 등록</legend>
        <% for (let i = 0; i < viewData.length; i++) { %>
          <input type="hidden" name="id" value="<%= viewData[0].id %>">
          <input type="text" name="name" placeholder="사용자 이름" value="<%= viewData[0].name %>"/>
          <br>
          <input type="text" name="comment" placeholder="방명록" value="<%= viewData[0].comment %>"/>
          <br>
        <% } %>
        <button type="submit">등록</button>
      </fieldset>
    </form>
    <p>
      <table border="1" cellspacing="0">
        <thead>
          <tr>
            <th>ID</th>
            <th>작성자</th>
            <th>방명록</th>
            <th>수정</th>
            <th>삭제</th>
          </tr>
        </thead>
        <tbody>
          <% for (let i = 0; i < data.length; i++) { %>
          <tr id="tr_<%= data[i].id %>">
            <td><%= data[i].id %></td>
            <td><%= data[i].name %></td>
            <td><%= data[i].comment %></td>
            <td><button type="button">수정</button></td>
            <td><button type="button">삭제</button></td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </p>
    
  </body>
</html>
