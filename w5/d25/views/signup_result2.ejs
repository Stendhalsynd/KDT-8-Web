<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      span {
        color: blue;
        font-weight: 700;
      }
    </style>
  </head>
  <body>
    <h1><%= title %></h1>
    <div>이름 : <span><%= userInfo.name %></span></div>
    <div>성별 : <span><%= userInfo.sex %></span></div>
    <div>
      생년월일 : <span><%= userInfo.year %></span>년
      <span><%= userInfo.month %></span>월 <span><%= userInfo.day %></span>일
    </div>
    <div>관심사 : <span><%= userInfo.favorate %></span></div>
    <div>선택 - 좋아하는 색상 : <span><%= userInfo.select[0] %></span></div>
    <div>선택 - 좋아하는 숫자 : <span><%= userInfo.select[1] %></span></div>
    <a href="/">홈으로 이동</a>
  </body>
</html>
