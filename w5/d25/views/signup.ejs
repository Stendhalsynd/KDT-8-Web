<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1><%= title %></h1>

    <form action="/signup" method="get" id="get">
      이름
      <input type="text" name="name" />
      <fieldset form="get">
        <legend>성별</legend>
        <label
          ><input type="radio" id="man" name="sex" value="남자" />남자<input
            type="radio"
            id="female"
            name="sex"
            value="여자"
          />여자</label
        >
      </fieldset>
      <br />
      <fieldset form="get">
        <legend>생년월일</legend>
        <label>
          <select name="year">
            <% for(let i = 1990; i < 2010; i++) { %>
            <option value="<%= i %>"><%= i %></option>
            <% } %>
          </select>
          년
          <select name="month">
            <% for(let i = 1; i <= 12; i++) { %>
            <option value="<%= i %>"><%= i %></option>
            <% } %>
          </select>
          월
          <select name="day">
            <% for(let i = 1; i <= 31; i++) { %>
            <option value="<%= i %>"><%= i %></option>
            <% } %>
          </select>
          일
        </label>
      </fieldset>
      <br />
      <fieldset form="get">
        <legend>관심사</legend>
        <label
          ><input type="checkbox" name="favorate" value="여행" />여행<input
            type="checkbox"
            name="favorate"
            value="패션"
          />패션<input
            type="checkbox"
            name="favorate"
            vlaue="음식"
          />음식</label
        >
      </fieldset>
      <br />
      <button type="submit">회원가입</button>
    </form>
  </body>
</html>
